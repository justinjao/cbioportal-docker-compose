deploy:
  commands: 
   - apt-get install -y wget
   - ./init.sh
   - okteto build -t okteto.dev/cbioportal-docker-compose-cbioportal:okteto-with-volume-mounts cbioportal
   - okteto exec cbioportal -- 'metaImport.py -u http://localhost:8080 -s study/lgg_ucsf_2014/ -o' --loglevel debug
   #  - gunzip *.sql.gz
  compose: docker-compose.yml
