deploy:
  commands: 
   - apt-get install -y wget
   - ./init.sh
  #  - gunzip *.sql.gz
   - ls config
   - cat config/portal.properties
  compose: docker-compose.yml

dev:
  test:
    command: ls config
